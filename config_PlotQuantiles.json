{
    
    // x1 and x2 are original variables used for making quantiles and grids
    "x1": "q3", // always choose x1 to be slice variable 
    "y1": "q0", 
  
    ////////////////////////////////////////////////////////////////////////////
    // Choose the desired group of variables (x2 and y2 are plotting variables):
    ////////////////////////////////////////////////////////////////////////////

    // "y2": "CosLep",
    // "x2": "PLep",
    // "x2Label": "#mu",
    // "y2Label": "Cos #theta",

    "x2": "PProton1",
    "y2": "CosProton",
    "x2Label": "Leading Proton",
    "y2Label": "Cos #theta",

    /////////////////////////////
    // Set the following options:
    /////////////////////////////

    "zaxis": true,
    "Norm": false,
    "max": 1000, // Choose max value integer (w/out quotation marks) or "None" (use quotation marks)
    "logz": true,

    ////////////////////////////////////////////
    // Choose the desired group of cut & labels:
    ////////////////////////////////////////////

    // Single Pion Events:

    // "cut": "flagCC0pi == 0 && (Mode == 11 || Mode ==  12 || Mode == 13 || Mode == 14 || Mode == 15 || Mode == 16)",
    // "mode_label": "CC1Pi",
    // "mode_title": "CC1Pi",


    // QE Events:

    // "cut": "flagCC0pi == 1 && Mode ==1",
    // "mode_label": "CC0Pi&QE",
    // "mode_title": "CC0Pi + QE",


    // 2P2H Events:

    "cut": "flagCC0pi == 1 && Mode ==2",
    "mode_label": "CC0Pi&2P2H",
    "mode_title": "CC0Pi + 2P2H",
  
    "plot_type": "segments"  // "grid" or "segments"
  }
  